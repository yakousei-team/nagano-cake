<div class="nav-img1"></div>
<div class="container">
  <div class="row frame">
    <div class="col-md-2"></div>
    <div class="col-md-8">
<h3>注文確認</h3>
  <div>
    <table class="table">
      <thead>
        <tr>
          <th>商品名</th>
          <th>単価(税込)</th>
          <th>数量</th>
          <th>小計</th>
        </tr>
      </thead>
      <tbody>
        <% @cart_items.each do |cart_item| %>
        <% @total += ((cart_item.item.unit_price * 1.1).floor * cart_item.quantity) %>
        <tr>
          <td><%= cart_item.item.name %></td>
          <td><%= (cart_item.item.unit_price * 1.1).floor %></td>
          <td><%= cart_item.quantity %></td>
          <td><%= ((cart_item.item.unit_price * 1.1).floor * cart_item.quantity) %></td>
        </tr>
        <% end %>
      </tbody>
    </table>
  </div>

    <table class="table">
      <thead>
        <tr><td>送料</td><td>800</td></tr>
        <tr><td>商品合計</td><td><%= @total %></td></tr>
        <tr><td>請求額</td><td><%= @total + 800 %></td></tr>
      </thead>
    </table>

  <h3>支払い方法</h3>
  <p><%= session[:order]["payment_method"] %></p>
  <h3>お届け先</h3>
  <div class="delivery-address">
  <div class="sideby">
      <P><%= session[:order]["postcode"] %></P></div>
  <div class="sideby">
      <p><%= session[:order]["address"] %></p></div>
  <div class="sideby">
      <p><%= session[:order]["name"] %></p></div>
        <%= button_to "購入を確定する", orders_path, method: :post, class: "btn btn-success pull-right" %></div>
</div>
</div>
</div>